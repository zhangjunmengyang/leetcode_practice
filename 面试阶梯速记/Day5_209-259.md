# 面试阶梯思路速记 Day 5（209-259）

> 范围：209-259，共 51 题。
> 难度：简单 0 / 中等 51 / 困难 0。
> 今日主线：滑窗、动态规划、贪心、树 DFS、图。
> 刷法：先背“一句话速记”，再默写代码骨架，最后补“更优/补充”。

---

## 209. 435 无重叠区间【中等】
- 套路：贪心｜分类：区间集合 / 动态规划 / 排序 / 数组
- 一句话速记：想保留最多区间，就优先留下结束最早的。
- 面试可过：按结束点排序，当前区间若和上一个保留区间重叠就删除它。
- 更优/补充：这是区间调度经典贪心。
```python
intervals.sort(key=lambda x: x[1])
end = intervals[0][1]
keep = 1
for s, e in intervals[1:]:
    if s >= end:
        keep += 1
        end = e
return len(intervals) - keep
```

## 210. 437 路径总和 III【中等】
- 套路：树 DFS｜分类：二叉树 / 二叉树 - 深度优先搜索 / 树 / 深度优先搜索
- 一句话速记：树题默认写 DFS：函数返回值管子树，参数管路径状态。
- 面试可过：递归后序最稳：先拿左右子树结果，再更新当前节点答案。
- 更优/补充：若题目强调层次、最短路、最近出口，再改 BFS。
```python
def dfs(node):
    if not node:
        return base
    left = dfs(node.left)
    right = dfs(node.right)
    return merge(left, right, node)
```

## 211. 438 找到字符串中所有字母异位词【中等】
- 套路：滑窗｜分类：滑动窗口 / 哈希表 / 字符串
- 一句话速记：固定窗口大小 = `len(p)`，窗口字符计数和目标计数相等就记答案。
- 面试可过：定长窗口维护 26 维计数，O(n)。
- 更优/补充：定长异位词窗口模板。
```python
need = Counter(p)
win = Counter()
ans = []
for i, ch in enumerate(s):
    win[ch] += 1
    if i >= len(p):
        old = s[i - len(p)]
        win[old] -= 1
        if win[old] == 0: del win[old]
    if win == need:
        ans.append(i - len(p) + 1)
return ans
```

## 212. 443 压缩字符串【中等】
- 套路：双指针｜分类：数组 / 字符串 / 双指针 / 字符串
- 一句话速记：压缩字符串就双指针分组：统计连续段长度后原地写回。
- 面试可过：慢指针负责写，快指针负责找段尾。
- 更优/补充：原地字符串数组处理常见模板。
```python
write = left = 0
while left < len(chars):
    right = left
    while right < len(chars) and chars[right] == chars[left]:
        right += 1
    ...
    left = right
return write
```

## 213. 450 删除二叉搜索树中的节点【中等】
- 套路：BST｜分类：二叉搜索树 / 二叉树 / 树
- 一句话速记：BST 删除分三种：0 个孩子直接删，1 个孩子顶替，2 个孩子找后继替换。
- 面试可过：删除后继续保持 BST 性质。
- 更优/补充：面试里把三种情况说明白就够。
```python
if key < root.val:
    root.left = deleteNode(root.left, key)
elif key > root.val:
    root.right = deleteNode(root.right, key)
else:
    ...
return root
```

## 214. 452 用最少数量的箭引爆气球【中等】
- 套路：贪心｜分类：区间 / 区间集合 / 排序 / 数组
- 一句话速记：射气球要尽量复用同一支箭，所以优先按右端点排序。
- 面试可过：当前气球起点若大于上一箭位置，就必须新开一箭。
- 更优/补充：区间最少点覆盖的经典模型。
```python
points.sort(key=lambda x: x[1])
ans = 1
end = points[0][1]
for s, e in points[1:]:
    if s > end:
        ans += 1
        end = e
return ans
```

## 215. 547 省份数量【中等】
- 套路：图｜分类：图 - 深度优先搜索 / 图 / 并查集 / 广度优先搜索
- 一句话速记：省份数量就是无向图连通块个数。
- 面试可过：DFS/BFS/并查集都行；面试里 DFS 最好写。
- 更优/补充：本质和岛屿数量一样。
```python
seen = set()
def dfs(i):
    for j, c in enumerate(isConnected[i]):
        if c and j not in seen:
            seen.add(j)
            dfs(j)
```

## 216. 560 和为 K 的子数组【中等】
- 套路：前缀和 + 哈希｜分类：子串 / 前缀和 / 哈希表 / 数组
- 一句话速记：子数组和 = k 就想前缀和：若 `pre[j]-pre[i]=k`，则找 `pre[i]=pre[j]-k`。
- 面试可过：哈希表统计前缀和出现次数，边走边累加答案，O(n)。
- 更优/补充：这是前缀和计数母题。
```python
cnt = Counter({0: 1})
pre = ans = 0
for x in nums:
    pre += x
    ans += cnt[pre - k]
    cnt[pre] += 1
return ans
```

## 217. 649 Dota2 参议院【中等】
- 套路：队列｜分类：队列 / 字符串 / 贪心
- 一句话速记：两派各维护一个队列，谁下标更小谁先出手，并把自己加到下一轮。
- 面试可过：队列模拟回合制博弈，O(n)。
- 更优/补充：比字符串反复删除高效。
```python
r = deque(i for i, ch in enumerate(senate) if ch == 'R')
d = deque(i for i, ch in enumerate(senate) if ch == 'D')
n = len(senate)
while r and d:
    a, b = r.popleft(), d.popleft()
    if a < b: r.append(a + n)
    else: d.append(b + n)
return 'Radiant' if r else 'Dire'
```

## 218. 714 买卖股票的最佳时机含手续费【中等】
- 套路：状态 DP｜分类：动态规划 - 多维 / 动态规划 / 数组 / 贪心
- 一句话速记：持股 / 不持股两状态滚动，手续费放在买或卖时扣都行。
- 面试可过：`hold` 表示当前持股最大收益，`cash` 表示不持股最大收益。
- 更优/补充：股票 DP 先背状态转移。
```python
hold, cash = -prices[0], 0
for p in prices[1:]:
    hold = max(hold, cash - p)
    cash = max(cash, hold + p - fee)
return cash
```

## 219. 735 小行星碰撞【中等】
- 套路：栈｜分类：栈 / 数组 / 模拟
- 一句话速记：只有“栈顶向右、当前向左”才会相撞。
- 面试可过：栈保存稳定状态，新小行星和栈顶不断比较直到决出胜负。
- 更优/补充：物理碰撞题常用栈模拟。
```python
st = []
for x in asteroids:
    alive = True
    while alive and x < 0 and st and st[-1] > 0:
        ...
    if alive:
        st.append(x)
return st
```

## 220. 739 每日温度【中等】
- 套路：单调栈｜分类：单调栈 / 栈 / 数组
- 一句话速记：想知道下一个更大元素，就维护单调递减栈。
- 面试可过：当前温度更高时，栈里比它低的都能结算答案。
- 更优/补充：单调栈母题。
```python
ans = [0] * len(temperatures)
st = []
for i, x in enumerate(temperatures):
    while st and temperatures[st[-1]] < x:
        j = st.pop()
        ans[j] = i - j
    st.append(i)
return ans
```

## 221. 763 划分字母区间【中等】
- 套路：贪心｜分类：贪心算法 / 双指针 / 哈希表 / 字符串
- 一句话速记：每个字符最后出现位置决定当前分段最远要扩到哪里。
- 面试可过：顺扫时维护本段最远右边界，走到边界就切一刀。
- 更优/补充：分段覆盖题的常见贪心。
```python
last = {ch: i for i, ch in enumerate(s)}
ans = []
start = end = 0
for i, ch in enumerate(s):
    end = max(end, last[ch])
    if i == end:
        ans.append(end - start + 1)
        start = i + 1
return ans
```

## 222. 790 多米诺和托米诺平铺【中等】
- 套路：DP｜分类：动态规划 - 一维 / 动态规划
- 一句话速记：铺砖题看状态机：完整铺满、缺一格等状态之间如何转移。
- 面试可过：记住标准递推即可，复杂度 O(n)。
- 更优/补充：面试里重点说清状态含义。
```python
full = [0] * (n + 1)
part = [0] * (n + 1)
full[0] = full[1] = 1
for i in range(2, n + 1):
    full[i] = full[i - 1] + full[i - 2] + 2 * part[i - 1]
    part[i] = part[i - 1] + full[i - 2]
return full[n] % MOD
```

## 223. 841 钥匙和房间【中等】
- 套路：图｜分类：图 - 深度优先搜索 / 图 / 广度优先搜索 / 深度优先搜索
- 一句话速记：房间和钥匙就是图的可达性问题。
- 面试可过：DFS/BFS 从 0 号房间出发，看能否访问所有节点。
- 更优/补充：连通性题模板。
```python
seen = {0}
q = deque([0])
while q:
    x = q.popleft()
    for y in rooms[x]:
        if y not in seen:
            seen.add(y); q.append(y)
return len(seen) == len(rooms)
```

## 224. 875 爱吃香蕉的珂珂【中等】
- 套路：二分答案｜分类：二分查找 / 数组
- 一句话速记：速度越快越容易吃完，答案满足单调性，直接二分速度。
- 面试可过：写 `check(k)` 统计总小时数是否 <= h，然后二分最小速度。
- 更优/补充：这类“最小满足条件”题都往二分答案想。
```python
def check(k):
    return sum((x + k - 1) // k for x in piles) <= h
l, r = 1, max(piles)
while l < r:
    mid = (l + r) // 2
    if check(mid): r = mid
    else: l = mid + 1
return l
```

## 225. 901 股票价格跨度【中等】
- 套路：单调栈｜分类：单调栈 / 数据流 / 栈 / 设计
- 一句话速记：股票跨度是往左找第一个比今天价格大的位置，所以维护价格递减栈。
- 面试可过：栈里存 `(price, span)`，弹掉不大于当前价格的项并累加跨度。
- 更优/补充：在线 next greater / previous greater 常见模板。
```python
span = 1
while st and st[-1][0] <= price:
    span += st.pop()[1]
st.append((price, span))
return span
```

## 226. 909 蛇梯棋【中等】
- 套路：BFS｜分类：图的广度优先搜索 / 广度优先搜索 / 数组 / 矩阵
- 一句话速记：棋盘游戏最少步数就是图最短路。
- 面试可过：每个格子向后 1..6 个位置连边，遇蛇梯先跳转，再 BFS。
- 更优/补充：关键是编号和矩阵坐标互转。
```python
q = deque([(1, 0)])
seen = {1}
while q:
    pos, step = q.popleft()
    for nxt in range(pos + 1, min(pos + 6, n * n) + 1):
        ...
```

## 227. 918 环形子数组的最大和【中等】
- 套路：动态规划｜分类：Kadane 算法 / 分治 / 动态规划 / 单调队列
- 一句话速记：DP 先定状态：`dp[i]` / `dp[i][j]` 表示什么，再写转移。
- 面试可过：先写初值，再按依赖顺序转移；能滚动就滚动压空间。
- 更优/补充：面试里把“状态、转移、初始化、遍历顺序”四句话说清就够。
```python
dp = init()
for i in range(...):
    for j in range(...):
        dp[i][j] = best(from_states)
return dp[-1][-1]
```

## 228. 994 腐烂的橘子【中等】
- 套路：BFS｜分类：图 - 广度优先搜索 / 图论 / 广度优先搜索 / 数组
- 一句话速记：多源 BFS：所有烂橘子一起入队，按层扩散。
- 面试可过：每层代表一分钟，统计剩余新鲜橘子是否清零。
- 更优/补充：这类“最短扩散时间”都想到多源 BFS。
```python
q = deque(all_rotten)
minute = 0
while q and fresh:
    for _ in range(len(q)):
        ...
    minute += 1
```

## 229. 1004 最大连续1的个数 III【中等】
- 套路：滑窗｜分类：滑动窗口 / 二分查找 / 前缀和 / 数组
- 一句话速记：窗口内最多允许 k 个 0；超过了就缩左边。
- 面试可过：典型“至多 k 个坏点”的窗口题，O(n)。
- 更优/补充：背住这个模板，很多题通用。
```python
l = zeros = ans = 0
for r, x in enumerate(nums):
    zeros += x == 0
    while zeros > k:
        zeros -= nums[l] == 0
        l += 1
    ans = max(ans, r - l + 1)
return ans
```

## 230. 1143 最长公共子序列【中等】
- 套路：二维 DP｜分类：动态规划 - 多维 / 多维动态规划 / 动态规划 / 字符串
- 一句话速记：LCS：字符相同就继承左上 + 1，不同就取上或左最大。
- 面试可过：经典二维 DP，O(mn)。
- 更优/补充：面试常问状态定义和转移。
```python
dp = [[0] * (n + 1) for _ in range(m + 1)]
for i in range(1, m + 1):
    for j in range(1, n + 1):
        if a[i - 1] == b[j - 1]:
            dp[i][j] = dp[i - 1][j - 1] + 1
        else:
            dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])
return dp[m][n]
```

## 231. 1161 最大层内元素和【中等】
- 套路：树 BFS｜分类：二叉树 - 广度优先搜索 / 二叉树 / 广度优先搜索 / 树
- 一句话速记：每层累加，记录层和最大的那一层编号。
- 面试可过：层序遍历时统计每层和，比较更新答案，O(n)。
- 更优/补充：DFS 也能做成 `depth -> sum`，但 BFS 更直接。
```python
ans, best = 1, -10**18
level, q = 1, deque([root])
while q:
    s = 0
    for _ in range(len(q)):
        node = q.popleft()
        s += node.val
        if node.left: q.append(node.left)
        if node.right: q.append(node.right)
    if s > best:
        best, ans = s, level
    level += 1
return ans
```

## 232. 1268 搜索推荐系统【中等】
- 套路：排序 / 字典树｜分类：前缀树 / 二分查找 / 堆（优先队列） / 字典树
- 一句话速记：面试先给排序 + 双指针 / 二分，字典树是加分项。
- 面试可过：产品名排序后，对每个前缀二分出左边界，往后拿前 3 个匹配项。
- 更优/补充：比硬写 Trie 更短。
```python
products.sort()
ans = []
pre = ''
l = 0
for ch in searchWord:
    pre += ch
    l = bisect_left(products, pre, l)
    ans.append([p for p in products[l:l+3] if p.startswith(pre)])
return ans
```

## 233. 1318 或运算的最小翻转次数【中等】
- 套路：位运算｜分类：位运算
- 一句话速记：位运算题就盯住 `& | ^ << >>` 和每一位是否独立。
- 面试可过：判奇偶、消重、统计 bit、异或分组，都是面试高频。
- 更优/补充：不会推就先把样例按二进制写出来。
```python
ans = 0
while n:
    ans += n & 1
    n >>= 1
return ans
```

## 234. 1372 二叉树中的最长交错路径【中等】
- 套路：树 DFS｜分类：二叉树 - 深度优先搜索 / 二叉树 / 动态规划 / 树
- 一句话速记：树题默认写 DFS：函数返回值管子树，参数管路径状态。
- 面试可过：递归后序最稳：先拿左右子树结果，再更新当前节点答案。
- 更优/补充：若题目强调层次、最短路、最近出口，再改 BFS。
```python
def dfs(node):
    if not node:
        return base
    left = dfs(node.left)
    right = dfs(node.right)
    return merge(left, right, node)
```

## 235. 1448 统计二叉树中好节点的数目【中等】
- 套路：树 BFS｜分类：二叉树 - 深度优先搜索 / 二叉树 / 广度优先搜索 / 树
- 一句话速记：树题先想层序 BFS：一层一层推，天然处理层信息。
- 面试可过：队列分层遍历，按题意统计每层答案，通常 O(n)。
- 更优/补充：如果还要路径/父子约束，再切 DFS 携带状态；层题优先 BFS。
```python
q = deque([root])
while q:
    for _ in range(len(q)):
        node = q.popleft()
        if node.left: q.append(node.left)
        if node.right: q.append(node.right)
```

## 236. 1456 定长子串中元音的最大数目【中等】
- 套路：滑窗｜分类：滑动窗口 / 字符串
- 一句话速记：定长窗口数元音，进一个减一个。
- 面试可过：元音集合 + 固定窗口，O(n)。
- 更优/补充：定长子串统计题标准模板。
```python
vowels = set('aeiou')
cur = sum(ch in vowels for ch in s[:k])
ans = cur
for i in range(k, len(s)):
    cur += s[i] in vowels
    cur -= s[i - k] in vowels
    ans = max(ans, cur)
return ans
```

## 237. 1466 重新规划路线【中等】
- 套路：图｜分类：图 - 深度优先搜索 / 图 / 广度优先搜索 / 深度优先搜索
- 一句话速记：无向遍历整棵树，但在边上记住原方向；遇到“背离 0”的边就要改向。
- 面试可过：建双向图，原正向边打 1，反向边打 0，DFS/BFS 累加需要翻转的边。
- 更优/补充：这是树遍历 + 方向标记题。
```python
g = defaultdict(list)
for a, b in connections:
    g[a].append((b, 1))
    g[b].append((a, 0))
```

## 238. 1493 删掉一个元素以后全为 1 的最长子数组【中等】
- 套路：滑窗｜分类：滑动窗口 / 动态规划 / 数组
- 一句话速记：等价于窗口内最多保留 1 个 0，答案是窗口长度减 1。
- 面试可过：和 `Max Consecutive Ones III` 同模板。
- 更优/补充：记得最后一定要删一个元素。
```python
l = zeros = ans = 0
for r, x in enumerate(nums):
    zeros += x == 0
    while zeros > 1:
        zeros -= nums[l] == 0
        l += 1
    ans = max(ans, r - l)
return ans
```

## 239. 1657 确定两个字符串是否接近【中等】
- 套路：哈希｜分类：哈希表 / 哈希集合 / 哈希表 / 字符串 / 排序
- 一句话速记：close 的条件：字符集合相同，且频次数组排序后相同。
- 面试可过：因为你只能重排字符和交换字符频次。
- 更优/补充：核心是“集合一致 + 频次 multiset 一致”。
```python
c1, c2 = Counter(word1), Counter(word2)
return set(c1) == set(c2) and sorted(c1.values()) == sorted(c2.values())
```

## 240. 1679 K 和数对的最大数目【中等】
- 套路：哈希 / 双指针｜分类：双指针 / 哈希表 / 排序 / 数组
- 一句话速记：面试先给哈希计数配对：来一个数就看补数库存够不够。
- 面试可过：一次遍历 O(n)。
- 更优/补充：若先排序，也能用双指针 O(n log n)。
```python
cnt = Counter()
ans = 0
for x in nums:
    if cnt[k - x]:
        cnt[k - x] -= 1
        ans += 1
    else:
        cnt[x] += 1
return ans
```

## 241. 1926 迷宫中离入口最近的出口【中等】
- 套路：BFS｜分类：图 - 广度优先搜索 / 广度优先搜索 / 数组 / 矩阵
- 一句话速记：无权最短步数直接 BFS，第一次走到边界出口就是答案。
- 面试可过：入口先标记访问，队列里存坐标和步数。
- 更优/补充：迷宫最短路模板。
```python
q = deque([(sx, sy, 0)])
maze[sx][sy] = '+'
while q:
    x, y, d = q.popleft()
    ...
```

## 242. 2095 删除链表的中间节点【中等】
- 套路：链表｜分类：链表 / 双指针
- 一句话速记：链表题先想 dummy + 双指针，改指针前先存 `nxt`。
- 面试可过：涉及删除/拼接就挂 dummy；涉及环或中点就快慢指针。
- 更优/补充：若还要回文/孪生和，常见套路是找中点后反转后半段。
```python
dummy = ListNode(0, head)
pre, cur = dummy, head
while cur:
    nxt = cur.next
    # 改指针前先存 nxt
    cur = nxt
```

## 243. 2130 链表最大孪生和【中等】
- 套路：链表｜分类：链表 / 双指针 / 栈
- 一句话速记：找中点，反转后半，前后一起扫求最大和。
- 面试可过：链表回文题的变形，O(n) / O(1)。
- 更优/补充：会回文链表，这题就顺手做出来。
```python
slow = fast = head
while fast and fast.next:
    slow = slow.next
    fast = fast.next.next
pre = None
while slow:
    nxt = slow.next
    slow.next = pre
    pre, slow = slow, nxt
ans = 0
while pre:
    ans = max(ans, head.val + pre.val)
    head, pre = head.next, pre.next
return ans
```

## 244. 2300 咒语和药水的成功对数【中等】
- 套路：排序 + 二分｜分类：二分查找 / 双指针 / 排序 / 数组
- 一句话速记：药水排序后，对每个咒语二分第一个满足乘积 >= success 的位置。
- 面试可过：排序一次，再对每个 spell 做 lower_bound，O((n+m)log m)。
- 更优/补充：比双重循环省太多。
```python
potions.sort()
ans = []
for x in spells:
    need = (success + x - 1) // x
    i = bisect_left(potions, need)
    ans.append(len(potions) - i)
return ans
```

## 245. 2336 无限集中的最小数字【中等】
- 套路：设计 + 堆｜分类：堆 / 优先队列 / 哈希表 / 堆（优先队列） / 有序集合
- 一句话速记：无限集本质是一个递增指针 + 一个“加回来的数”的最小堆。
- 面试可过：没被弹出过的最小值由指针给，`addBack` 过的值由堆给。
- 更优/补充：注意用 set 防重复加回。
```python
if heap and heap[0] < cur:
    return heappop(heap)
cur += 1
return cur - 1
```

## 246. 2352 相等行列对【中等】
- 套路：哈希｜分类：哈希表 / 哈希集合 / 哈希表 / 数组 / 模拟
- 一句话速记：把每一行序列当 key 计数，再枚举每一列去查。
- 面试可过：行转 tuple 存 Counter，列也转 tuple 匹配，O(n^2)。
- 更优/补充：矩阵行列匹配题标准解。
```python
rows = Counter(tuple(row) for row in grid)
ans = 0
for col in zip(*grid):
    ans += rows[tuple(col)]
return ans
```

## 247. 2390 从字符串中移除星号【中等】
- 套路：栈｜分类：栈 / 字符串 / 模拟
- 一句话速记：星号就删掉前一个字符，天然用栈。
- 面试可过：普通字符入栈，遇 `*` 就 pop。
- 更优/补充：纯模拟题。
```python
st = []
for ch in s:
    if ch == '*':
        st.pop()
    else:
        st.append(ch)
return ''.join(st)
```

## 248. 2462 雇佣 K 位工人的总代价【中等】
- 套路：堆｜分类：堆 / 优先队列 / 双指针 / 堆（优先队列） / 数组
- 一句话速记：左右两端各维护一个候选堆，每次取更便宜的一边，再从同侧补人。
- 面试可过：典型双堆模拟，O(k log candidates)。
- 更优/补充：注意左右指针别交叉重复入堆。
```python
# left_heap / right_heap
# 每次弹更小的，再从对应一侧补一个
```

## 249. 2542 最大子序列的分数【中等】
- 套路：排序 + 堆｜分类：堆 / 优先队列 / 堆（优先队列） / 排序 / 数组
- 一句话速记：按 `nums2` 从大到小枚举“当前最小 nums2”，同时用小根堆保最大的 k 个 `nums1` 之和。
- 面试可过：答案 = 当前 `nums2` * 堆里 `nums1` 总和。
- 更优/补充：这是“固定最小值 + top k 和”模型。
```python
pairs = sorted(zip(nums2, nums1), reverse=True)
heap = []
s = ans = 0
for b, a in pairs:
    heappush(heap, a); s += a
    if len(heap) > k:
        s -= heappop(heap)
    if len(heap) == k:
        ans = max(ans, s * b)
return ans
```

## 250. LCR 121 寻找目标值 - 二维数组【中等】
- 套路：二分 / 矩阵｜分类：查找算法 / 二分查找 / 分治 / 数组｜原型：二维有序矩阵查找
- 一句话速记：从右上角出发：大了往左，小了往下，一步排掉一行或一列。
- 面试可过：利用行列双有序，复杂度 O(m+n)。
- 更优/补充：这是二维有序矩阵查找标准走法。
```python
i, j = 0, len(matrix[0]) - 1
while i < len(matrix) and j >= 0:
    if matrix[i][j] == target: return True
    if matrix[i][j] > target: j -= 1
    else: i += 1
return False
```

## 251. LCR 124 推理二叉树【中等】
- 套路：分治｜分类：分治算法 / 二叉树 / 分治 / 哈希表
- 一句话速记：前序第一个是根，在中序里找到它，就能切出左右子树范围。
- 面试可过：哈希表记录中序下标，递归建左右子树，O(n)。
- 更优/补充：构树题核心是“一个遍历给根，另一个遍历给区间”。
```python
pos = {x: i for i, x in enumerate(inorder)}
def build(pl, pr, il, ir):
    if pl > pr: return None
    root = TreeNode(preorder[pl])
    k = pos[preorder[pl]]
    left = k - il
    root.left = build(pl + 1, pl + left, il, k - 1)
    root.right = build(pl + left + 1, pr, k + 1, ir)
    return root
```

## 252. LCR 129 字母迷宫【中等】
- 套路：回溯｜分类：搜索与回溯算法 / 回溯 / 字符串 / 数组
- 一句话速记：单词搜索就是 DFS + 回溯：从每个起点出发，走一步就标记已访问。
- 面试可过：递归匹配 `word[k]`，四连通扩展，失败就撤销访问标记。
- 更优/补充：常用原地改字符代替 visited 数组，空间更省。
```python
def dfs(i, j, k):
    if board[i][j] != word[k]: return False
    if k == len(word) - 1: return True
    ch, board[i][j] = board[i][j], '#'
    for ni, nj in dirs:
        if ok and dfs(ni, nj, k + 1): return True
    board[i][j] = ch
    return False
```

## 253. LCR 130 衣橱整理【中等】
- 套路：动态规划｜分类：搜索与回溯算法 / 动态规划 / 广度优先搜索 / 深度优先搜索｜原型：机器人的运动范围
- 一句话速记：DP 先定状态：`dp[i]` / `dp[i][j]` 表示什么，再写转移。
- 面试可过：先写初值，再按依赖顺序转移；能滚动就滚动压空间。
- 更优/补充：面试里把“状态、转移、初始化、遍历顺序”四句话说清就够。
```python
dp = init()
for i in range(...):
    for j in range(...):
        dp[i][j] = best(from_states)
return dp[-1][-1]
```

## 254. LCR 131 砍竹子 I【中等】
- 套路：动态规划｜分类：数学 / 动态规划｜原型：剪绳子 I
- 一句话速记：DP 先定状态：`dp[i]` / `dp[i][j]` 表示什么，再写转移。
- 面试可过：先写初值，再按依赖顺序转移；能滚动就滚动压空间。
- 更优/补充：面试里把“状态、转移、初始化、遍历顺序”四句话说清就够。
```python
dp = init()
for i in range(...):
    for j in range(...):
        dp[i][j] = best(from_states)
return dp[-1][-1]
```

## 255. LCR 132 砍竹子 II【中等】
- 套路：动态规划｜分类：数学 / 动态规划｜原型：剪绳子 II
- 一句话速记：DP 先定状态：`dp[i]` / `dp[i][j]` 表示什么，再写转移。
- 面试可过：先写初值，再按依赖顺序转移；能滚动就滚动压空间。
- 更优/补充：面试里把“状态、转移、初始化、遍历顺序”四句话说清就够。
```python
dp = init()
for i in range(...):
    for j in range(...):
        dp[i][j] = best(from_states)
return dp[-1][-1]
```

## 256. LCR 134 Pow(x, n)【中等】
- 套路：快速幂｜分类：分治算法 / 数学 / 递归
- 一句话速记：指数每次折半，当前位是 1 就把底数乘进答案。
- 面试可过：二进制快速幂，O(log n)。
- 更优/补充：负指数先转成正指数，再最后取倒数。
```python
if n < 0:
    x, n = 1 / x, -n
ans = 1
while n:
    if n & 1:
        ans *= x
    x *= x
    n >>= 1
return ans
```

## 257. LCR 138 有效数字【中等】
- 套路：状态机 / 模拟｜分类：字符串
- 一句话速记：有效数字题规则碎，最稳是有限状态机或分情况布尔标记。
- 面试可过：面试先给标记法：是否见过数字、小数点、指数。
- 更优/补充：若面试官追问严谨性，再说 DFA。
```python
seen_num = seen_dot = seen_e = False
for i, ch in enumerate(s.strip()):
    ...
return seen_num
```

## 258. LCR 143 子结构判断【中等】
- 套路：树 DFS｜分类：搜索与回溯算法 / 二叉树 / 树 / 深度优先搜索｜原型：树的子结构
- 一句话速记：树题默认写 DFS：函数返回值管子树，参数管路径状态。
- 面试可过：递归后序最稳：先拿左右子树结果，再更新当前节点答案。
- 更优/补充：若题目强调层次、最短路、最近出口，再改 BFS。
```python
def dfs(node):
    if not node:
        return base
    left = dfs(node.left)
    right = dfs(node.right)
    return merge(left, right, node)
```

## 259. LCR 148 验证图书取出顺序【中等】
- 套路：栈｜分类：模拟 / 数组 / 栈
- 一句话速记：按 pushed 顺序入栈，只要栈顶能匹配 popped 就不断弹。
- 面试可过：完整模拟一遍，最后栈空说明合法，O(n)。
- 更优/补充：这是入栈出栈序列模板题。
```python
st = []
j = 0
for x in pushed:
    st.append(x)
    while st and j < len(popped) and st[-1] == popped[j]:
        st.pop()
        j += 1
return not st
```
