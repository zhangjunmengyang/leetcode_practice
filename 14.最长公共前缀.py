#
# @lc app=leetcode.cn id=14 lang=python3
#
# [14] 最长公共前缀
#

# @lc code=start
class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        st = ""
        l, num = len(strs[0]), len(strs)
        # 最长不超过 l
        # 破坏条件，如果任意的字符的下一个 char 跟现在的不相等，或者超长了，则直接结束。
        for i in range(l):
            

                
        
# @lc code=end

